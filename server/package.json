{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "bun run --cwd ../shared build",
    "build": "bun exec tsc",
    "dev": "bun --watch run src/index.ts & bun exec tsc --watch"
  },
  "dependencies": {
    "hono": "^4.10.8",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
